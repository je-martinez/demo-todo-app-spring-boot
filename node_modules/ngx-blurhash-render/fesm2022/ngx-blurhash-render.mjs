import { isPlatformBrowser, CommonModule, NgOptimizedImage } from '@angular/common';
import * as i0 from '@angular/core';
import { PLATFORM_ID, Component, Inject, Input, ViewChild } from '@angular/core';
import { decode } from 'blurhash';

/**
 * Blurhash rendering component for Angular 15+
 */
class NgxBlurhashComponent {
    blurHashValue;
    /**
     * The blurHash string to render on the Canvas
     */
    get blurHash() {
        return this.blurHashValue;
    }
    set blurHash(value) {
        this.blurHashValue = value;
        this.decodeBlurHash();
    }
    /**
     * The image src string to render after fully loaded
     */
    imageSrcValue;
    get imageSrc() {
        return this.imageSrcValue;
    }
    set imageSrc(value) {
        this.imageSrcValue = value;
    }
    /**
     * The loading of the image based on its position in the DOM. By default loading is set to 'eager'
     */
    loading = 'eager';
    /**
     * Border Radius for the image in px. Default value is 0px
     */
    borderRadius = '0';
    /**
     * The width of the image and canvas in px. Default value is 200px
     */
    width = '200px';
    /**
     * The height of the image and canvas in px. Default value is 200px
     */
    height = '200px';
    imageLoaded = false;
    imageLoad = false;
    canvas;
    canvasWidth = 100;
    canvasHeight = 100;
    isBrowser;
    constructor(platformId) {
        this.isBrowser = isPlatformBrowser(platformId);
    }
    ngAfterViewInit() {
        this.decodeBlurHash();
    }
    decodeBlurHash() {
        if (this.canvas && this.blurHash) {
            const context = this.canvas.nativeElement.getContext('2d');
            const imageData = context?.createImageData(this.canvasWidth, this.canvasHeight);
            const pixels = decode(this.blurHash, this.canvasWidth, this.canvasHeight);
            imageData?.data.set(pixels);
            if (imageData)
                context?.putImageData(imageData, 0, 0);
        }
    }
    static ɵfac = i0.ɵɵngDeclareFactory({ minVersion: "12.0.0", version: "17.3.12", ngImport: i0, type: NgxBlurhashComponent, deps: [{ token: PLATFORM_ID }], target: i0.ɵɵFactoryTarget.Component });
    static ɵcmp = i0.ɵɵngDeclareComponent({ minVersion: "14.0.0", version: "17.3.12", type: NgxBlurhashComponent, isStandalone: true, selector: "ngx-blurhash-render", inputs: { blurHash: "blurHash", imageSrc: "imageSrc", loading: "loading", borderRadius: "borderRadius", width: "width", height: "height" }, viewQueries: [{ propertyName: "canvas", first: true, predicate: ["canvas"], descendants: true }], ngImport: i0, template: `
    <canvas
      #canvas
      [width]="canvasWidth"
      [height]="canvasHeight"
      [style.borderRadius]="borderRadius"
      [style.width]="width"
      [style.height]="height"
    ></canvas>
    <img
      alt="blurred-image"
      [ngSrc]="imageSrc"
      width="100"
      height="100"
      (load)="imageLoad = true"
      (onloadeddata)="imageLoaded = true"
      [class.img-loaded]="imageLoad"
      [style.borderRadius]="borderRadius"
      [style.width]="width"
      [style.height]="height"
    >
  `, isInline: true, styles: [":host{display:block;position:relative}canvas{height:100%;width:100%;position:absolute;left:0}img{opacity:0;width:100%;height:100%;position:absolute;object-fit:cover;left:0}.img-loaded{animation:popIn .4s both ease-in}@keyframes popIn{0%{opacity:0}to{opacity:1}}\n"], dependencies: [{ kind: "ngmodule", type: CommonModule }, { kind: "directive", type: NgOptimizedImage, selector: "img[ngSrc]", inputs: ["ngSrc", "ngSrcset", "sizes", "width", "height", "loading", "priority", "disableOptimizedSrcset", "fill", "src", "srcset"] }] });
}
i0.ɵɵngDeclareClassMetadata({ minVersion: "12.0.0", version: "17.3.12", ngImport: i0, type: NgxBlurhashComponent, decorators: [{
            type: Component,
            args: [{ selector: 'ngx-blurhash-render', standalone: true, imports: [CommonModule, NgOptimizedImage], template: `
    <canvas
      #canvas
      [width]="canvasWidth"
      [height]="canvasHeight"
      [style.borderRadius]="borderRadius"
      [style.width]="width"
      [style.height]="height"
    ></canvas>
    <img
      alt="blurred-image"
      [ngSrc]="imageSrc"
      width="100"
      height="100"
      (load)="imageLoad = true"
      (onloadeddata)="imageLoaded = true"
      [class.img-loaded]="imageLoad"
      [style.borderRadius]="borderRadius"
      [style.width]="width"
      [style.height]="height"
    >
  `, styles: [":host{display:block;position:relative}canvas{height:100%;width:100%;position:absolute;left:0}img{opacity:0;width:100%;height:100%;position:absolute;object-fit:cover;left:0}.img-loaded{animation:popIn .4s both ease-in}@keyframes popIn{0%{opacity:0}to{opacity:1}}\n"] }]
        }], ctorParameters: () => [{ type: Object, decorators: [{
                    type: Inject,
                    args: [PLATFORM_ID]
                }] }], propDecorators: { blurHash: [{
                type: Input
            }], imageSrc: [{
                type: Input
            }], loading: [{
                type: Input
            }], borderRadius: [{
                type: Input
            }], width: [{
                type: Input
            }], height: [{
                type: Input
            }], canvas: [{
                type: ViewChild,
                args: ['canvas']
            }] } });

/*
 * Public API Surface of ngx-blurhash-render
 */

/**
 * Generated bundle index. Do not edit.
 */

export { NgxBlurhashComponent };
//# sourceMappingURL=ngx-blurhash-render.mjs.map
